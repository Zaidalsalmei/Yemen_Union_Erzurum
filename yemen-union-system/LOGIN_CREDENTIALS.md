# 🔐 بيانات تسجيل الدخول التجريبية

## حساب المسؤول الرئيسي (Main Admin)

### البيانات:
```
رقم الهاتف: 05376439960
كلمة المرور: Admin@123456
```

## حساب المسؤول الثانوي (Secondary Admin)

### البيانات:
```
رقم الهاتف: 05350703570
كلمة المرور: password
```

## كيفية تسجيل الدخول:

### 1. افتح المتصفح
```
http://localhost:5176
```

### 2. أدخل البيانات
- **رقم الهاتف**: `05350703570`
- **كلمة المرور**: `password`

### 3. اضغط "تسجيل الدخول"

---

## إذا واجهت مشكلة:

### الخطأ: "رقم الهاتف أو كلمة المرور غير صحيحة"
**الحل**: تأكد من:
- رقم الهاتف: `05350703570` (11 رقم)
- كلمة المرور: `password` (حروف صغيرة)

### الخطأ: "فشل الاتصال بالخادم"
**الحل**: تأكد من:
1. الخادم الخلفي يعمل على المنفذ 8000
2. الخادم الأمامي يعمل على المنفذ 5176
3. قاعدة البيانات تعمل

### الخطأ: "CORS Error"
**الحل**: 
- أعد تشغيل الخادم الخلفي
- امسح الـ cache (Ctrl+Shift+R)

---

## التحقق من الخوادم:

### الخادم الأمامي (Frontend):
```bash
# يجب أن يكون يعمل على:
http://localhost:5176
```

### الخادم الخلفي (Backend):
```bash
# يجب أن يكون يعمل على:
http://localhost:8000

# للتحقق:
curl http://localhost:8000/api/test
```

### قاعدة البيانات:
```bash
# التحقق من المستخدم:
C:\xampp\mysql\bin\mysql.exe -u root yemen_union_db -e "SELECT full_name, phone_number, status FROM users WHERE phone_number='05350703570';"
```

---

## بعد تسجيل الدخول:

### ما يجب أن تراه:
1. ✅ لوحة التحكم (Dashboard)
2. ✅ القائمة الجانبية على اليمين
3. ✅ الإحصائيات في 4 بطاقات
4. ✅ الأنشطة القادمة
5. ✅ طلبات العضوية

### اختبار التصميم المتجاوب:
1. اضغط F12 (DevTools)
2. اضغط Ctrl+Shift+M (Device Mode)
3. اختر أحجام مختلفة:
   - iPhone SE (375px)
   - iPad (768px)
   - Desktop (1440px)

### على الموبايل:
- ✅ القائمة مخفية
- ✅ زر القائمة (☰) في الأعلى
- ✅ اضغط على الزر لفتح القائمة
- ✅ القائمة تنزلق من اليمين
- ✅ طبقة شفافة خلف القائمة

---

## الصلاحيات:

حساب المسؤول لديه جميع الصلاحيات:
- ✅ عرض لوحة التحكم
- ✅ إدارة الأعضاء (عرض، إضافة، تعديل، حذف)
- ✅ إدارة الأنشطة
- ✅ إدارة الاشتراكات
- ✅ إدارة المالية
- ✅ إدارة التقارير
- ✅ إدارة الإعدادات

---

## إنشاء حساب جديد:

إذا أردت إنشاء حساب جديد:
1. اضغط "إنشاء حساب جديد"
2. املأ البيانات المطلوبة
3. تحقق من رقم الهاتف عبر OTP
4. سجل الدخول

**ملاحظة**: نظام OTP يتطلب API Key صالح من Wasender

---

## 🎉 استمتع بالتطبيق!

التطبيق الآن:
- ✅ متجاوب بالكامل
- ✅ يعمل على جميع الأجهزة
- ✅ جاهز للاستخدام

**رقم الهاتف**: `05350703570`  
**كلمة المرور**: `password`
